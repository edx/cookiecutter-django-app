# Core requirements for using this package template

cookiecutter              # Utility to generate a package from this template
watchdog                  # Used in the "watch" and "replay" make targets
